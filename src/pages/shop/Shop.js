import { Grid, Typography } from "@mui/material";

function Shop() {
  return (
    <Grid container>
      <Grid item>
        <Typography variant="h3">Shop</Typography>
      </Grid>
    </Grid>
  );
}

export default Shop;
