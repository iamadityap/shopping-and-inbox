import { Grid, Typography } from "@mui/material";

function Inbox() {
  return (
    <Grid container>
      <Grid item>
        <Typography variant="h3">Inbox</Typography>
      </Grid>
    </Grid>
  );
}

export default Inbox;
