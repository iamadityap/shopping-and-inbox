import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#2502c5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default theme;
